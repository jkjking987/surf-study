{
  "name": "surf-study",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/ci-env.mjs",
    "build": "astro build --remote",
    "postbuild": "node scripts/post-build.mjs",
    "build:local": "astro build",
    "build:standalone": "node scripts/build-standalone.mjs",
    "preview": "astro preview",
    "serve": "node scripts/serve.mjs",
    "astro": "astro",
    "check": "astro check",
    "convert": "node scripts/json-to-content.mjs"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^11.2.0",
    "@astrojs/db": "^0.14.3",
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^4.16.0"
  }
}
